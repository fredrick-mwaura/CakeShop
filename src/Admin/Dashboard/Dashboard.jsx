import React from 'react';

const Dashboard = () => {
   return (
      <div>
          <h2>Dashboard</h2> {/* implement the google analytics and display its response(traffic) */}
      </div>
   );
}

export default Dashboard;
